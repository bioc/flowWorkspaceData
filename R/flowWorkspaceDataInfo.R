flowWorkspaceDataInfo <-
function(){
cat("This package contains fcs data files, xml workspaces and GatingSets for testing the flowWorkspace and openCyto packages and building the their vignettes.\n These can be located in the inst/extdata directory of the flowWorkspaceData package.\n The sample files and workspaces contain data from whole blood. The primary purpose of this data set is to test the import code and export code in the flowWorkspace package and running automated gating pipeline in the openCyto package.")}

